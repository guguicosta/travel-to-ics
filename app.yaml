# Google Cloud App Engine configuration
runtime: python39

instance_class: F1

env_variables:
  SECRET_KEY: "your-secret-key-here"

handlers:
- url: /static
  static_dir: static

- url: /.*
  script: auto

automatic_scaling:
  target_cpu_utilization: 0.65
  min_instances: 0
  max_instances: 5
